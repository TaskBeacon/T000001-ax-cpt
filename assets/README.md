# AX-CPT Assets

This task uses PsychoPy text primitives for cue and probe letters. No external
visual stimulus assets are required for the canonical AX-CPT implementation.

Voice files may be generated from config-defined participant instructions during
human runs when voice output is enabled.
